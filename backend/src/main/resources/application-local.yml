spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_tickerboard?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8080
